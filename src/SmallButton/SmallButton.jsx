const SmallButton = ({buttontext}) => {
    return (
        <button className="btn btn-primary text-base font-medium text-white rounded-full bg-linear-to-r from-[#4F39F6] to-[#9514FA]">{buttontext}</button>
    );
};

export default SmallButton;